# WorldEngine SAM2 worker

Optional local box-prompted segmentation worker for Studio compiles. Install a reviewed SAM 2.1 package and Hiera Small checkpoint separately, then pin its repository commit and checkpoint SHA-256 in the provider policy. The worker performs no downloads.
